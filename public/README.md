# public/ assets

Static assets served by Next.js live here.

The v1 hero uses an abstract CSS/SVG visual, so no mascot or brand icon asset is
needed. Add future project screenshots here when replacing the generated
case-study mockups with real captures.
